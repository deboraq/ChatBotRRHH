rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Toda la app usa Firebase Admin SDK / cuenta de servicio en el servidor.
    // El cliente web no debe leer ni escribir Firestore directamente.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
